import { DefaultTheme } from "@react-navigation/native";

export const TestnetTheme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: "#e64a19",
    },
};

export const MainnetTheme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: "#e64a19",
    },
};
